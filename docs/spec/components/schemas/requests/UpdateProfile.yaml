type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "account id"
      type:
        type: string
        enum: [ update_profile ]
      attributes:
        type: object
        properties:
          pseudonym:
            type: string
            description: "pseudonym"
          description:
            type: string
            description: "description"
          avatar_key:
            type: string
            description: "avatar media key"