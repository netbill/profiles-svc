type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "account id"
      type:
        type: string
        enum: [ profile ]
      attributes:
        type: object
        required:
          - official
        properties:
          official:
            type: boolean
            description: "official"